import {NgForFilterSomePipe} from './ng-for-filter-some.pipe';

describe('NgForFilterSomePipe', () => {
  it('create an instance', () => {
    const pipe = new NgForFilterSomePipe();
    expect(pipe).toBeTruthy();
  });
});
