import {NgForFilterEveryPipe} from './ng-for-filter-every.pipe';

describe('NgForFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NgForFilterEveryPipe();
    expect(pipe).toBeTruthy();
  });
});
